{{-- @states sinh biến + setter; {{ }} trở thành output có marker hydrate --}}
@states({
    count: 0,
    name: 'Sao'
})
<template>
    <div class="counter">
        <span>{{ name }} đã bấm {{ count }} lần</span>
        <button @click(setCount(count + 1))>Tăng</button>
    </div>
</template>
